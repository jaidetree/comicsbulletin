<?php
/**
 * This file is used to hide the columns from the listing
 *
 * Just copy one of the lines and edit between the single
 * quotes in the slugify('<editable content>')
 */

/**
 * Don't edit this line.
 */
$hide_columns = array();

/** 
 * Edit these lines
 *
 * Copy and paste the column names from the footer of the 
 * frontend site.
 * This way it's the exact name from the database.
 */
$hide_columns[] = slugify('Cigarette Burns');
$hide_columns[] = slugify('DudeBro Comics');
$hide_columns[] = slugify('Cinebooks');
$hide_columns[] = slugify('Comic Effect');
$hide_columns[] = slugify('For Kids and Tweens');
$hide_columns[] = slugify('Learning Curve');
$hide_columns[] = slugify('The Park and Barb Show');
$hide_columns[] = slugify('Pulp Fiction');
$hide_columns[] = slugify('Pulp Never Dies');
$hide_columns[] = slugify('Secret Origins');
$hide_columns[] = slugify('Streaming Pile o\' Wha?');
?>
